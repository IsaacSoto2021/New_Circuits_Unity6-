using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LockpickMinigame : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


    /*
     when run into door
    create random order of 4 numbers
    display list of four numbers (flash one at a time)
    accept input
    if input correct, proceed to open door
    if input wrong, door stay locked
     
     */

}
